node_modules/
*.log
dist/
legacy/

# Cache d'import dell'editor Godot: rigenerata in automatico all'apertura del progetto,
# non va mai versionata (per lo stesso motivo gli zip di consegna la escludono sempre).
godot/.godot/
